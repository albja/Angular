import { Li1Directive } from './li1.directive';

describe('Li1Directive', () => {
  it('should create an instance', () => {
    const directive = new Li1Directive();
    expect(directive).toBeTruthy();
  });
});
